# 安全策略

## 支持范围

安全修复以最新正式版本和默认分支为主。较旧版本可能只提供升级建议。

## 报告漏洞

请使用代码托管平台仓库 **Security** 页面中的私密漏洞报告功能。仓库公开前，维护者
应先启用 Private vulnerability reporting。若该入口不可用，请通过仓库所有者公开资料
中列出的私密联系方式联系维护者；不要创建包含漏洞细节的公开 Issue。

报告中请尽量包含受影响版本、Windows 版本、复现步骤、影响、最小化日志和建议修复。
请勿提交真实用户输入、剪贴板内容、截图、用户数据库、证书或其他个人信息。能够安全
脱敏时，使用虚构数据复现。

维护者会先确认收到报告，再评估严重程度、修复范围和披露时间。修复发布前，请避免
公开利用细节。

## 安全边界

项目保护本地持久化数据和输入法进程间通信，但不能隔离管理员、已入侵的同用户会话，
也不能阻止具有相同用户权限的恶意程序模拟输入或读取系统剪贴板。截图文件本身不是
DPAPI 加密数据。完整边界见 README 的“隐私与安全”章节。
