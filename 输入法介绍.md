# 开心输入法：把中文输入留在自己的电脑里

开心输入法是一款面向 Windows 10 和 Windows 11 的开源、本地优先中文输入法。它通过 Windows 原生 TSF 框架接入系统，支持日常办公、聊天、写作、编程以及全屏应用等场景。拼音解析、候选生成、用户学习、词库查询、手写识别和 OCR 均可在本机完成，核心输入功能不依赖账号、云端服务或网络连接。

## 为什么做一款本地优先的输入法

我们开发这款输入法，是因为输入法与普通软件不同。它几乎参与用户每一次文字表达，可能接触聊天内容、工作资料、搜索关键词、代码、地址甚至隐私信息。一个值得长期使用的输入法，不仅要“打得准、反应快”，还应让用户知道数据保存在哪里、哪些功能正在运行，以及如何随时关闭、导出或删除这些数据。开心输入法希望提供的，正是一套透明、可控、能够被社区检查的本地输入方案。

## 不只是把拼音转换成汉字

开心输入法支持全拼、简拼以及全拼与声母混合输入，能够同时生成整句、长词、短词和单字候选。候选排序综合考虑字符二元模型（Bigram）、基础词频、上下文、用户选择频次和近期输入记录，在保持常用词稳定性的同时逐步适应个人习惯。

面对真实输入中常见的错按、漏按、按键顺序颠倒和近音问题，输入法提供邻键、漏键、调序及拼音纠错能力，并支持 `z/zh`、`c/ch`、`s/sh`、`n/l`、`f/h`、`an/ang`、`en/eng`、`in/ing` 等可选模糊音。项目还内置双拼方案、简繁输出、自定义短语、英文单词、日期时间、符号和 Emoji 候选。学习过的词条可以置顶、移除、导入、明文导出或全部清空，用户始终保留对个人词库的控制权。

候选窗口支持横排和竖排显示，每页可配置三至九个候选，并提供经典、紧凑和卡片等布局。用户可以调整字体、字号、字重、透明度、间距、圆角、浅色或深色外观，也可以直接选择项目内置的多套主题。针对高分辨率屏幕、高对比度环境、全屏程序和游戏，输入法提供增强定位与独立候选覆盖层，还可以按应用设置缩放比例、显示器、候选位置和提交方式。

在兼容性方面，开心输入法除了标准 TSF 提交，还提供 Unicode SendInput、剪贴板粘贴等可选策略，以适配传统软件、终端工具、游戏和部分特殊编辑器。安装包同时包含 64 位与 32 位 TSF 组件，用于兼容不同位数的 Windows 宿主程序。

## 输入之外的本地工具箱

开心输入法内置了一组围绕文字处理设计的辅助工具，包括剪贴板管理器、手写查字、区域或窗口截图、本地 OCR、托盘设置以及快捷文本助手。

剪贴板管理器支持搜索、置顶、复制、粘贴和快速调用；手写工具可在独立画布中查找生僻字；截图功能可以使用 Windows Graphics Capture，也可与 ShareX 集成；OCR 基于 RapidOCR 和 ONNXRuntime，在本机完成文字检测与识别，并支持图片预处理、二次框选、历史记录和结果整理。

通过 `vv` 直输助手，用户还可以直接生成日期、时间、星期、中文数字、人民币大写、百分比、容量单位、计算结果、常用符号、Emoji、邮箱、网址和 Markdown 片段，无须离开当前编辑窗口。

翻译属于可选的外部联动功能。开心输入法可以通过本地命名管道连接另行安装的 WinTranslator，但本仓库不打包翻译模型或翻译运行时。翻译是否联网以及如何处理数据，由 WinTranslator 自身决定，不属于开心输入法离线核心的承诺范围。

## 本地优先，也明确说明安全边界

隐私保护不是一句“数据不会上传”就可以概括的口号。开心输入法允许用户配置或关闭剪贴板后台采集、工具全局热键和运行诊断，并会把密码输入框和常见密码管理器识别为敏感上下文。用户也可以为指定程序设置“永不学习”“永不读取剪贴板”或“永不显示候选”。

启用全局隐私模式后，输入法会强制切换到 ASCII 输入、隐藏候选窗口、停止用户学习并禁止剪贴板访问。用户词库、剪贴板历史、OCR 历史和截图库索引等本地数据库使用 Windows DPAPI 保护；进程间通信通过受访问控制的本地命名管道和能力令牌校验完成，并拒绝远程管道客户端。

项目同时明确披露保护边界：配置文件为明文，截图文件是普通 PNG 或 JPEG，诊断日志也可能包含明文信息；管理员权限、已经被入侵的同用户会话，以及具有相同用户权限的恶意程序，不在输入法能够完全隔离的范围内。我们认为，坦诚说明限制比给出无法兑现的“绝对安全”承诺更重要。

## 面向用户，也面向开发者

开心输入法采用 C++/Win32 与 Rust 组合架构。C++ 前端负责 TSF 接入、按键处理、输入组合、候选窗口和应用兼容；Rust 引擎负责拼音解析、候选排序、用户学习、词库以及辅助工具。两部分职责清晰，既保留 Windows 原生输入体验，也便于对算法和工具持续迭代。

项目提供固定候选质量用例、学习回放、输入延迟统计、Rust 单元测试、C++/CTest 测试、安装检查和开源发布检查。仓库也为词库与第三方组件建立来源、许可证和生成记录，方便复现、审查和改进。自研代码与文档采用 Apache License 2.0，ShareX、RapidOCR、手写数据及其他第三方内容继续遵循各自的上游许可证。

当前版本为 1.0.0，正式支持 64 位 Windows 10 和 Windows 11。Windows on ARM、Windows Server、Wine 以及早于 Windows 10 的系统暂不在支持范围内。项目提供纯输入法与包含本地 OCR 的两种安装包；开发者也可以使用 Python、Rust、MSVC、CMake、PowerShell 和 Inno Setup 从源码完成构建、测试与打包。

开心输入法仍在持续成长。如果你在意离线输入、数据控制、Windows 原生兼容，或者希望研究拼音解析、候选排序、TSF、OCR 与本地工具的实现，欢迎下载体验、提交 Issue、改进文档或参与开发。我们希望它不仅是一款能够每天使用的输入法，也能成为一个透明、可验证、真正由用户掌控的中文输入项目。
